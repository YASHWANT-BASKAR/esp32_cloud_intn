services:
  - type: web
    name: esp32-fastapi
    env: python
    repo: https://github.com/YOUR_GITHUB_USERNAME/esp32-cloud-server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
